package com.java8Feature.lamdademo;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.Comparator;

public class NumberDecendingOrder {
    public static void main(String[] args) {
        //wap store number in arraylist order and sort number in descending order with lamda


        //wap store number in arraylist order and sort number in descending order without lamda
/*
        ArrayList<Integer> al=new ArrayList<>();
        al.add(4);
        al.add(1);
        al.add(5);
        al.add(2);

     System.out.println("before sort "+al);
        Collections.sort(al,new NewComparator());
        System.out.println("after sort "+al);

    }
}
class NewComparator implements Comparator<Integer> {
    @Override
    public int compare(Integer o1, Integer o2) {
        if(o1>o2) return -1;
        else if(o1<o2) return 1;
        else return 0;*/
    }
}

